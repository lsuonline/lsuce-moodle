define("block_backadel/migrate_status",["exports","./repository","core/notification"],(function(_exports,_repository,_notification){var obj;
/**
   * Live status polling for the Backadel filesystem migration adhoc task.
   *
   * On page load: immediately polls to check if a migration task is already queued.
   * While queued: disables the run button and polls every 5 s.
   * When task disappears: shows a success banner with updated counts.
   *
   * @module     block_backadel/migrate_status
   * @copyright  2026 Louisiana State University
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_notification=(obj=_notification)&&obj.__esModule?obj:{default:obj};let pollTimer=null,wasQueued=!1;const poll=()=>{(0,_repository.getMigrateStatus)().then((r=>((status,data)=>{const btn=document.querySelector('[data-action="run-migrate"]'),cntCat=document.querySelector('[data-region="migrate-count-catalogue"]'),cntCourses=document.querySelector('[data-region="migrate-count-courses"]'),successBadge=document.querySelector('[data-region="migrate-success"]');cntCat&&(cntCat.textContent=data.catalogue_count),cntCourses&&(cntCourses.textContent=data.courses_count),"queued"===status?(wasQueued=!0,btn&&(btn.disabled=!0),successBadge&&successBadge.classList.add("d-none"),pollTimer||(pollTimer=setInterval(poll,5e3))):(btn&&(btn.disabled=!1),pollTimer&&(clearInterval(pollTimer),pollTimer=null),successBadge&&(wasQueued||Number(data.catalogue_count)>0||Number(data.courses_count)>0)&&successBadge.classList.remove("d-none"))})(r.status,r))).catch(_notification.default.exception)};_exports.init=()=>{poll()}}));

//# sourceMappingURL=migrate_status.min.js.map